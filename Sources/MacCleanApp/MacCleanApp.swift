import SwiftUI

struct MacCleanApp: App {
    var body: some Scene {
        MenuBarExtra("Mac Clean", systemImage: "externaldrive") {
            Text("Mac Clean")
        }
    }
}

MacCleanApp.main()
